{
    "cmake.configureOnOpen": true,
    "cmake.buildDirectory": "${workspaceFolder}/build",
    "cmake.configureSettings": {
        "CMAKE_BUILD_TYPE": "${buildType}"
    },
    "cmake.debugConfig": {
        "name": "Debug ${projectName}",
        "type": "cppdbg",
        "request": "launch",
        "program": "${buildDirectory}/${projectName}",
        "args": [],
        "cwd": "${workspaceFolder}",
        "environment": [],
        "externalConsole": true,
        "MIMode": "gdb"
    }
}
